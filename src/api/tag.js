var express = require('express'),
    db = require('../db'),
    utils = require('../utils');
    
var app = module.exports = express();

/*
    /tag/list
    /rename-tag
    /edit-tag
    /disable-tag
*/

