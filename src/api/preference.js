var express = require('express'),
    db = require('../db'),
    utils = require('../utils');
    
var app = module.exports = express();

app.get('/reader/api/0/preference/list', function(req, res) {
    
});

app.get('/reader/api/0/preference/set', function(req, res) {
    
});

app.get('/reader/api/0/preference/stream/list', function(req, res) {
    
});

app.get('/reader/api/0/preference/stream/set', function(req, res) {
    
});